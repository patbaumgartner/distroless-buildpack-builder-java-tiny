# Build artifacts
*.tar
*.tar.gz

# Local pack cache
.pack/

# Docker layer cache
.docker/

# Editor files
.idea/
*.iml
.vscode/*
!.vscode/settings.json
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Java build artifacts.
# `target/` (unanchored) covers every sample plus the repository root, where
# `mvn -f samples/<x>/pom.xml -Drewrite.exportDatatables=true` writes its
# datatable CSVs — those are CI reports, never source.
target/
.mvn/wrapper/maven-wrapper.jar

# Temporary test artifacts
/tmp/
